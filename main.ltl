// ltl-mvcc-toy/main.ltl
//
// Multi-version concurrency control demo on toy rows.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-mvcc-toy v0.1.0")
    io::println("Multi-version concurrency control demo on toy rows.")
    0
}
