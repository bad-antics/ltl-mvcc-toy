// examples/demo.ltl — runnable demo for ltl_mvcc_toy

import std::io
use ltl_mvcc_toy as lib

fn main() {
    io::println("ltl_mvcc_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}